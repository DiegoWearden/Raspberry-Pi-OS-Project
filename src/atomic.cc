#include "atomic.h"
#include "utils.h"

#include "printf.h"

void pause()
{
    // if (Debug::shutdown_called) {
    //     Debug::panic("shutdown called\n");
    // }
    //  asm volatile("pause");
}
